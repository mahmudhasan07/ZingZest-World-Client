import { useParams } from "react-router-dom";
// import { useParams } from "react-router-dom";

const Item = () => {
    const id = useParams()
    console.log(id);
    return (

        <section>
            <div>
                
            </div>
        </section>
    );
};

export default Item;