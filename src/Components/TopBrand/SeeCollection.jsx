

const SeeCollection = ({hide, brand}) => {
    return (
        <div className={`w-full h-full ${props}`}>
            <h1>See the Collection</h1>
            <button></button>
        </div>
    );
};

export default SeeCollection;