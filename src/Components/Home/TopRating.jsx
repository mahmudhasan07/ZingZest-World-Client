import useFetch from "../Hooks/useFetch";

const TopRating = () => {
    const [data, refetch] = useFetch
    return (
        <section>
            
        </section>
    );
};

export default TopRating;